# Uncomment the next line to define a global platform for your project
 platform :ios, '10.0'

target 'emo' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for omav
  pod 'Firebase/Core', '~>4.0', :modular_headers => true
  pod 'Firebase/Messaging', '~>4.0', :modular_headers => true
  pod 'FirebaseCore', :modular_headers => true
  pod 'SDWebImagePhotosPlugin'
  pod 'BoringSSL-GRPC', :modular_headers => false
  pod 'gRPC-Core', :modular_headers => false
  pod "CZPicker"
  pod "HVTableView"
  pod 'BlurryModalSegue'
  pod 'UIImage+BlurredFrame'
  pod 'ACEDrawingView'
  target 'emoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'emoUITests' do
    inherit! :search_paths
    # Pods for testing
  end
 
end
